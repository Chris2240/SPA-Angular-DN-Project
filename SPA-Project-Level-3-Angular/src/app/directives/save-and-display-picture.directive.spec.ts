import { SaveAndDisplayPictureDirective } from './save-and-display-picture.directive';

describe('SaveAndDisplayPictureDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveAndDisplayPictureDirective();
    expect(directive).toBeTruthy();
  });
});
