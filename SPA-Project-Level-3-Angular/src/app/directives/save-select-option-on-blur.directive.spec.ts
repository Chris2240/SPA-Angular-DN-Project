import { SaveSelectOptionOnBlurDirective } from './save-select-option-on-blur.directive';

describe('SaveSelectOptionOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveSelectOptionOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
