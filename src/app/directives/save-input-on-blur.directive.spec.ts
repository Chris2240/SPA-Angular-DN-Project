import { SaveInputOnBlurDirective } from './save-input-on-blur.directive';

describe('SaveInputOnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveInputOnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
